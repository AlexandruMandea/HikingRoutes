export enum UserRole {
    ADMIN = 'admin',
    USER = 'user',
}

export enum RouteUnit {
    METRIC = 'metric',
    IMPERIAL = 'imperial',
}

export enum TravelMode {
    BICYCLING = 'BICYCLING',
    WALKING = 'WALKING',
}